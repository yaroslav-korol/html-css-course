**/.DS_Store ### Feature 2 CONFLICT LINE
# keep playing with git
# keep going
# and going
# trying create separate branch by -am commit
# clone the project as separate developer
# adding ssh key to remote